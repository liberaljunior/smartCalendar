<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.smartcalendar">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logoooo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logoooo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:windowSoftInputMode="stateHidden">
        <activity android:name=".PiGraphView"></activity>
        <activity android:name=".addDueDate" />
        <activity android:name=".DailyCheckList" />
        <activity android:name=".weekView" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>